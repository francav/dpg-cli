// SPDX-License-Identifier: Apache-2.0
// Copyright 2026 Victor França

/**
 * Placeholder export proving the @dpg/cli toolchain builds.
 * Replaced with real command implementations in a later work unit.
 */
export const greet = (name: string): string => `Hello ${name} from @dpg/cli`;
